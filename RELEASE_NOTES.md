# 视频资源整理 v2.8.1（Build 27）

发布日期：2026-08-13

## 本版修复

- **修复用户主页解析错乱**：此前在主页作品列表接口被风控拦截时，会兜底抓取页面上的所有作品链接，导致把推荐流中其他用户的视频误当成该主页的作品。现在：
  - 作品链接只从该用户的作品网格容器内收集，页面全局链接不再作为作品来源；
  - 新增从主页服务端渲染数据（SSR）读取该用户公开作品列表，接口被拦截时依然能得到正确作品；
  - 接口与 SSR 都拿不到数据时明确报错并附来源诊断，不再输出未经归属确认的内容。
- **修复打包版启动白屏**：PyInstaller 应用包内符号链接导致 WebKit 无法加载页面，现已在加载前解析到真实文件。
- 更新仓库主页界面截图。

## 本版验证

- Python 完整回归测试：83 项通过（含新增 SSR 合并与推荐流污染回归测试）。
- 构建产物不依赖 Anaconda；ZIP 完整性与 SHA-256 校验通过；codesign 验证通过。
- 实际打开 `.app` 验证主界面正常渲染。

## 发行说明

- 自包含应用：内置 Python 运行时，不需要用户安装 Python。
- 仅支持 Apple Silicon（arm64）；最低 macOS 12；Windows 版本暂未提供。
- ad-hoc 临时签名，尚未 Apple 公证；其他 Mac 首次打开若提示未识别开发者，请右键选择“打开”。
- 校验方法：`shasum -a 256 视频资源整理-v2.8.1-macOS-arm64.zip`，与 Release 附带的 `SHA256SUMS.txt` 比对。
